<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Kreska pozioma (górna) -->
    <item
        android:left="0dp"
        android:right="0dp"
        android:top="1dp"
        android:bottom="0dp">
        <shape android:shape="line">
            <stroke
                android:color="@color/black"
            android:width="2dp" />
        </shape>
    </item>

    <!-- Kreska pionowa (lewa) -->
    <item
        android:left="1dp"
        android:right="0dp"
        android:top="0dp"
        android:bottom="0dp">
        <shape android:shape="line">
            <stroke
                android:color="@color/black"
            android:width="2dp" />
        </shape>
    </item>
</layer-list>
